info:
  name: register
  type: http
  seq: 6

http:
  method: POST
  url: "{{baseUrl}}/api/auth/register"
  body:
    type: json
    data: |-
      {
        "name": "Isaac Fungulwe",
        "email": "isaac@school.edu",
        "password": "Password123!",
        "role": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
