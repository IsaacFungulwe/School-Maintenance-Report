info:
  name: FetchingTickets
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:5000/api/tickets
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{jwtToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
